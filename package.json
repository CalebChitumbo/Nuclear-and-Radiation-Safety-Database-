{
  "name": "rpa-system",
  "version": "1.0.0",
  "description": "RPA Integrated Regulatory Information System \u2014 Radiation Protection Authority of Zambia",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:rules": "firebase emulators:exec --config firebase.rules-test.json --only firestore --project demo-rpa-rules \"vitest run --config vitest.rules.config.ts\"",
    "seed": "tsx scripts/seed.ts",
    "audit:screening": "tsx scripts/screening-audit.ts",
    "fix:duplicate-screening": "tsx scripts/collapse-duplicate-screening.ts",
    "seed:fresh": "tsx scripts/seed.ts --fresh",
    "seed:emulator": "FIRESTORE_EMULATOR_HOST=localhost:8080 tsx scripts/seed.ts",
    "seed:fresh:emulator": "FIRESTORE_EMULATOR_HOST=localhost:8080 tsx scripts/seed.ts --fresh",
    "emulator": "firebase emulators:start --import=./.emulator-data --export-on-exit",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes",
    "deploy:functions": "cd functions && npm run build && cd .. && firebase deploy --only functions",
    "deploy:hosting": "firebase deploy --only hosting"
  },
  "dependencies": {
    "clsx": "2.1.1",
    "firebase": "10.13.2",
    "firebase-admin": "12.5.0",
    "next": "14.2.13",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/node": "20.16.5",
    "@types/react": "18.3.7",
    "@types/react-dom": "18.3.0",
    "@vitejs/plugin-react": "4.3.1",
    "autoprefixer": "10.4.20",
    "eslint": "8.57.0",
    "eslint-config-next": "14.2.13",
    "postcss": "8.4.47",
    "tailwindcss": "3.4.12",
    "tsx": "4.19.1",
    "typescript": "5.6.2",
    "vitest": "2.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
